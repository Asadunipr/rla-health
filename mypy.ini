[mypy]
python_version = 3.11
ignore_missing_imports = True
explicit_package_bases = True
mypy_path = .
# Restrict what mypy checks (avoid treating files as top-level scripts)
files = rla, scripts

# tests often mix numpy dtypes/dynamic fixtures—relax there if needed:
[mypy-tests.*]
ignore_errors = True
